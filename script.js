const btn=document.querySelector('.menu-btn');const nav=document.querySelector('.navlinks');if(btn)btn.addEventListener('click',()=>nav.classList.toggle('open'));document.querySelectorAll('.navlinks a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));

const gallery=[...document.querySelectorAll('.gallery-item')];const lb=document.getElementById('lightbox');if(lb&&gallery.length){let idx=0;const img=lb.querySelector('img');const show=i=>{idx=(i+gallery.length)%gallery.length;img.src=gallery[idx].dataset.full;lb.classList.add('open');lb.setAttribute('aria-hidden','false')};gallery.forEach((b,i)=>b.addEventListener('click',()=>show(i)));lb.querySelector('.lightbox-close').onclick=()=>{lb.classList.remove('open');lb.setAttribute('aria-hidden','true')};lb.querySelector('.lightbox-prev').onclick=()=>show(idx-1);lb.querySelector('.lightbox-next').onclick=()=>show(idx+1);lb.addEventListener('click',e=>{if(e.target===lb)lb.querySelector('.lightbox-close').click()});document.addEventListener('keydown',e=>{if(!lb.classList.contains('open'))return;if(e.key==='Escape')lb.querySelector('.lightbox-close').click();if(e.key==='ArrowLeft')show(idx-1);if(e.key==='ArrowRight')show(idx+1)})}
